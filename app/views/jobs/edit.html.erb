<%= form_for @job do |f| %>
  <%= f.text_field :title %>
  <%= f.collection_select(:company_id, Company.all, :id, :name) %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', '/jobs' %>
